spring:
  datasource:
    url: jdbc:h2:mem:cherish;MODE=MYSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  session:
    jdbc:
      store-type: jdbc
      table-name: SPRING_SESSION
  jpa:
    hibernate:
      ddl-auto: create
    database: h2

logging:
  config:


